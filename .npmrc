# Vue 3.6 (which ships Vapor mode) is still a release candidate, while the
# @astrojs/vue integration declares a peer range of ^3.5.24. Allow the install
# to proceed with the pinned Vue 3.6 RC that Vapor requires.
legacy-peer-deps=true
